name: github_colors
description: A package for convert github language to color.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:

dev_dependencies:
  dio: 2.1.0
  yaml: 2.1.15

executables:
  github_colors: main